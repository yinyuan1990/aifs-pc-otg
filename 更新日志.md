# 更新日志

---

## 2026-04-11

### 🆕 新增功能

#### 1. 流量整形（Traffic Shaping）— winshaper 子进程
- 基于 WinDivert 的网络流量整形功能，可**按进程名**精确限制指定进程的**上传和下载速度**
- **双向限速已实现**：`uploadBps`（上传字节/秒）和 `downloadBps`（下载字节/秒）均已完整支持
- 令牌桶（Token Bucket）算法，上传/下载各维护独立令牌桶和队列，互不干扰
- 通过 WinDivert SOCKET 层建立 TCP 连接 → PID 映射（flowmap），再在 NETWORK 层按 PID 匹配规则限速
- 支持自动超时：`durationSec` 到期自动终止 winshaper 子进程
- 支持手动停止：`TRAFFIC_SHAPE_STOP` 立即终止
- **部署方式二选一**：
  - A) 内嵌编译（推荐）：`ZJC_EMBED_WINSHAPER=ON`，zjc_worker.exe 以 `--zjc-shaper` 参数自身拉起整形
  - B) 外部进程：winshaper.exe 与 zjc_worker.exe 同目录
- 两种方式均需管理员权限（WinDivert 驱动要求）

#### 2. 流量整形 STOMP 协议
- 后端通过以下频道下发指令：
  - `/topic/pc/{pcDeviceId}/shaper` — 单设备
  - `/topic/shaper/all` — 广播
- 开始限速：`{"action":"TRAFFIC_SHAPE", "taskId":"...", "processName":"chrome.exe", "uploadBps":102400, "downloadBps":204800, "durationSec":60}`
- 停止限速：`{"action":"TRAFFIC_SHAPE_STOP", "taskId":"..."}`
- 客户端执行结果上报到 `/app/subprocess/shaper-result`
- 与原有 BURST（带宽爆发）完全并行，互不影响

#### 3. 全量更新诊断日志（qlgx.txt）
- 在 `autoupdater.cpp` 全量更新流程中添加完整日志，写入应用目录下 `qlgx.txt`
- 覆盖以下关键环节：
  - 检查更新：URL、网络请求结果
  - 版本解析：当前版本 vs 最新版本、updateMode、下载地址
  - 下载过程：进度（25%/50%/75%/100%）、文件大小、错误信息
  - bat 脚本执行：taskkill、解压、xcopy/copy 每步的 errorlevel
  - 全量覆盖模式：zip 文件大小、解压目录内容列表、嵌套目录检测
  - 更新后验证：Phoenix.exe / zjc_worker.exe 文件信息
- 便于远程排查全量更新失败问题

#### 4. 流量整形支持 UDP + IPv6
- WinDivert NETWORK 层 filter 从 `"tcp"` 升级为 `"tcp or udp"`，同时拦截 TCP 和 UDP 流量
- 去除 IPv6 数据包的跳过逻辑，IPv4 和 IPv6 均参与限速
- `flowmap` 新增 `flowmap_lookup_ipv4` / `flowmap_lookup_ipv6` 通用查找函数，取代旧的 `flowmap_lookup_tcp_ipv4`
- `shape_run` 同时解析 TCP 头和 UDP 头，提取端口后统一走 flowmap 查 PID → 规则匹配 → 令牌桶限速
- SOCKET 层 flowmap 本身已监听所有协议（filter=`"true"`），对 UDP connected socket 的 CONNECT/CLOSE 事件自动建立 PID 映射

#### 5. zjc_worker 服务化（Windows Service）
- `zjc_worker.exe` 从普通自启动程序升级为 **Windows 服务**，以 LocalSystem（SYSTEM）权限运行
- **WinDivert 不再需要 UAC**：服务天然拥有管理员权限，流量整形开箱即用
- 命令行支持：
  - `zjc_worker.exe --install` — 安装并启动服务（需管理员）
  - `zjc_worker.exe --uninstall` — 停止并卸载服务
  - `zjc_worker.exe --standalone` — 强制独立模式（调试用）
  - 无参数启动时自动尝试服务模式，失败则回退独立模式
- 服务名 `zjc_worker`，自启动（AUTO_START），开机即运行
- 安装时自动清除旧的 HKCU\Run 注册表项
- 停止事件改为 `Global\zjc_worker_stop`（跨会话，服务 session 0 ↔ 用户 session 1+）
- `gracefulExit` 不再调用 `ExitProcess`，改为设置退出标志让主循环正常返回
- **Phoenix 交互更新**：
  - 启动时不再 kill zjc_worker，改为检查服务是否已安装，未安装则自动执行 `--install`
  - 退出时不再 launch zjc_worker，服务持续运行，只写 `zjc_auth.json` 供服务读取
- `install.bat` 更新为调用 `zjc_worker.exe --install` 注册服务
- **后端无需任何改动**，STOMP 协议和消息格式完全不变

- **文件**: `zjc_worker.cpp`, `main.cpp`, `pack.bat`, `pack2.bat`

### ⚠️ 待实现 / 后续计划

#### 流量整形
- ~~**UDP 限速**~~：已实现（2026-04-11）
- ~~**IPv6 限速**~~：已实现（2026-04-11）
- **多规则同时生效**：rules 文件支持多行多进程，但 zjc_worker 当前每次 TRAFFIC_SHAPE 只传一个 processName，重新发命令会先 kill 旧 winshaper 再起新的
- **实时统计上报**：目前只上报启动成功/失败，不上报实时限速统计（已传输字节、丢包等）
- **进程动态感知**：winshaper 运行期间若目标进程重启（新 PID），flowmap 会自动跟踪新连接，但已有连接的 PID 映射不会更新
- **UDP 无连接场景**：WinDivert SOCKET 层对 `sendto()`（不调用 `connect()`）的 UDP socket 只能捕获 BIND 事件（仅有本地地址），无法建立完整 5 元组 PID 映射。绝大多数应用（浏览器 QUIC、WebRTC、游戏客户端）使用 connected UDP，可正常限速

- **文件**: `autoupdater.cpp`, `winshaper/shape.c`, `winshaper/flowmap.c`, `winshaper/flowmap.h`, `zjc_worker.cpp`, `BACKEND_STOMP_SHAPER.txt`

---

## 2026-04-04

### 🆕 新增功能

#### 1. 爆发进程白名单检查
- 登录接口 `POST /api/auth/login/control` 响应新增 `processNames` 字段（字符串数组）
- 收到 BURST 指令时，先检查系统进程列表中是否存在白名单中的任一进程
- **有匹配** → 正常执行爆发，日志打印 `Process matched: xxx.exe`
- **无匹配** → 跳过爆发，向服务器上报 `skipped=true, reason=no_required_process`
- **白名单为空** → 无限制，直接爆发
- 支持灵活格式：后端可发 `Kirin` 或 `Kirin.exe`，均能正确匹配系统进程 `Kirin.exe`（不区分大小写）

#### 2. 主程序自动注册子进程开机自启
- Phoenix.exe 启动时自动将 `zjc_worker.exe` 注册到 Windows 启动项（`HKCU\...\Run`）
- 同时去除 `zjc_worker.exe` 和 `Phoenix.exe` 的 Zone.Identifier（网络下载标记），**消除开机启动时的安全提示弹窗**
- 只需运行一次 Phoenix，开机自启即生效，**不再需要手动执行 `install.bat`**

- **文件**: `zjc_worker.cpp`, `main.cpp`

---

## 2026-04-03

### 🆕 新增功能

#### 1. 子进程开机自启动
- `zjc_worker.exe` 每次启动时自动注册到 Windows 启动项（`HKCU\...\Run`），实现开机自启
- 无需用户手动配置，Windows 登录后自动运行

#### 2. 无账号自动注册
- 子进程启动时若无 `zjc_auth.json`（首次开机 / 从未登录主程序），自动执行注册流程：
  - 生成 `pcDeviceId`（与主程序算法一致：MachineGuid + MAC → SHA256）
  - 派生用户名 `zjc_{deviceHash}`、密码 `Zjc@{deviceHash}`
  - 调用 `POST /api/auth/register/control` 注册
  - 保存凭证到 `zjc_auth.json`，继续正常登录
- 用户后续在主程序登录后，主程序退出时会覆盖 `zjc_auth.json` 为真实账号

- **文件**: `zjc_worker.cpp`, `CMakeLists.txt`

---

## 2026-04-02

### 🆕 新增功能

#### 1. 子进程守护机制 (zjc_worker) — WebSocket 控制版
- 子进程 `zjc_worker.exe` 主程序关闭后自动启动，主程序打开时自动终止
- **独立登录**：读取 `zjc_auth.json`（主程序退出时写入），POST 登录获取独立 Token
- **WebSocket + STOMP**：连接 `wss://ws.147258yql.cn/ws`，携带 `clientType=subprocess`
  - 订阅 `/topic/pc/{pcDeviceId}/burst`（单点指令）
  - 订阅 `/topic/burst/all`（广播指令）
- **服务器控制爆发**：收到 `{"action":"BURST","duration":N}` 即触发一次带宽爆发（96 线程并发下载，支持 1-120 秒）
- **状态上报**：连接后每 60 秒上报 idle 状态，爆发时上报 bursting，完成后上报 burst-result
- **心跳**：每 10 秒发送 STOMP 心跳，断线自动重连
- 纯 Win32 + Winsock2 + WinHTTP，不依赖 Qt

#### 2. 带宽爆发性能优化
- **96 线程**并发下载，覆盖 6 个 CDN 源（QQ、微信、百度、360 + Hetzner、OVH 备用）
- **Burst 期间断线自动重连**：下载线程连接断开后立即切换到下一个 CDN 源继续下载，不浪费线程
- **Socket 调优**：2 MB 接收缓冲区、TCP_NODELAY、SO_ERROR 连接验证、512 KB 读取缓冲
- **连接超时 3 秒**（快速失败切换）、接收超时参数化
- **70% Ready 阈值**：等待大多数线程连接就绪后统一开火
- 实测可打满可用带宽（Speedtest 46 Mbps → Burst 48 Mbps，**100%+ 利用率**）

#### 3. 主子进程在线状态区分
- **主进程** WebSocket URL 携带 `pcDeviceId` + `clientType=main`
- **子进程** WebSocket URL 携带 `pcDeviceId` + `clientType=subprocess`
- 后端通过 Redis `pc:online:{pcDeviceId}` 的值区分在线状态

#### 4. 子进程优雅退出
- 主进程启动时通过 Windows Named Event (`Local\zjc_worker_stop`) 通知子进程
- 子进程收到信号后发送 `status=offline` + STOMP DISCONNECT，后端**秒级**感知状态变化
- 500ms 等待后兜底 `TerminateProcess` 强杀

#### 5. 主程序退出保存凭证
- `main.cpp` 退出时将 `{username, password, pcDeviceId, pcLevel}` 写入 `zjc_auth.json`
- 仅在有有效登录凭证时才启动子进程

#### 6. 后端接口规范文档
- 新增 `docs/zjc_backend_spec.md`，定义：
  - 子进程登录接口（`clientType: subprocess`）
  - WebSocket 连接 + STOMP 频道
  - 爆发指令消息格式（单点 + 广播）
  - 状态上报 + 爆发结果回报
  - 总代理后台定时任务配置（按小时、可配次数、单发/群发）
  - 主子进程在线状态区分及优雅退出机制

#### 7. 打包脚本更新
- `pack.bat` 和 `pack2.bat` 新增复制 `zjc_worker.exe` 到发布目录
- 更新时需使用 `updateMode=1`（全量更新）以投递子进程 exe

- **文件**: `zjc_worker.cpp`（新增）, `main.cpp`, `websocketclient.cpp`, `CMakeLists.txt`, `pack.bat`, `pack2.bat`, `docs/zjc_backend_spec.md`（新增）

---

## 2026-03-20

### 🆕 新增功能

#### 1. 列预览大幅升级
- **元素数量过滤**：仅当列中 item 个数为 2-5 时才弹出列预览（1 个或 ≥6 个不处理）
- **智能网格布局**：2-3 个 item 单行排列；4 个 item 2×2 布局；5 个 item 2×3 布局，最大化元素面积
- **拉伸开关**：顶部栏新增「拉伸:开/关」按钮。开=铺满（Stretch）；关=等比适应（PreserveAspectFit）
- **元素编号**：每个元素右上角显示圆形编号标签
- **操作按钮**：顶部栏显示 `Z ◀`（上一列）和 `▶ X`（下一列）按钮
- **悬停效果**：鼠标悬停时边框变为绿色加粗，底部浮现「按 A 放大」提示
- **A 键放大**：列预览中悬停元素后按 `A` 可单独放大查看（新层级 z:1002），支持滚轮切帧、S+滚轮缩放、←→切帧，再按 A 或 ESC 关闭
- **层级设计**：列预览放大(z:1002) > 列预览网格(z:1001) > 全屏查看(z:1000) > 截图grid，谁在最上面快捷键服务于谁
- **文件**: `MainPage.qml`

#### 2. 红色说明文字字号调整
- 相机设定弹窗 4 条说明文字（对焦/清晰度/帧率/超级帧率）字号从 22px 调整为 15px
- **文件**: `MainPage.qml`

#### 3. 档位按钮精简
- 去掉所有档位按钮下方的分辨率文字，只显示中文名称
- 在超低网按钮前添加"分辨率"提示文字
- 未开通的档位保持灰色形态（`#E8E8E8` 背景 + `#999999` 文字）
- 按钮恢复为单行圆角样式（60×32，radius: 16）
- **文件**: `MainPage.qml`

#### 4. 鼠标左右键切帧（所有切帧元素统一）
- **左键=上一帧，右键=下一帧**，适用于以下所有支持切帧的 UI 元素：
  - 截图 grid item（保留 Ctrl+点击列预览）
  - 慢放视频区（原左键抓拍改为切帧，抓拍改用 Space / 实时流区域）
  - 全屏查看图片（A 键放大后的图片）
  - 列预览每张图片
  - 列预览 A 键放大图片
- **层级优先**：上层覆盖时只服务上层（列预览放大 > 列预览 > 全屏查看 > 截图grid）
- **实时流视频区**保持左键=抓拍不变（无帧可切）
- **文件**: `MainPage.qml`

---

## 2026-03-19

### 🆕 新增功能

#### 1. 档位按钮显示分辨率（已移除）
- ~~相机设定弹窗中每个档位按钮下方显示对应分辨率~~ → 已在 2026-03-20 精简为只显示中文名称
- **文件**: `MainPage.qml`

#### 2. 快捷键说明按钮 & AI自动识别按钮
- 顶部工具栏「面板色」后新增「快捷键说明」按钮，点击打开快捷键说明弹窗
- 新增「AI自动识别」按钮，点击提示"敬请期待"
- **文件**: `MainPage.qml`

### ⚡ 优化

#### 1. 帧率与超级帧率顺序互换
- 相机设定弹窗中「帧率」移至「清晰度」之后（第3行），「超级帧率」移至「帧率」之后（第4行）
- 描述说明文字跟随移动

#### 2. 红色说明文字字体放大一倍
- 相机设定弹窗中所有红色提示/说明文字字号从 11→22、12→24（放大一倍）
- **文件**: `MainPage.qml`

---

## 2026-03-18

### 🆕 新增功能

#### 1. 数字键 1-9 列预览
- 按数字键 `1-9` 可预览对应列的所有截图，按窗口高度等比放大全屏显示
- 支持横向布局和纵向布局的列映射
- 再次按同一数字键或 `ESC` 关闭预览
- 顶部显示列号和图片数量提示
- **文件**: `MainPage.qml`

#### 2. 新增「超低网」挡位 (low)
- 相机设定弹窗新增「超低网」清晰度挡位，分辨率 640×480
- 实时流底部挡位切换同步添加
- 等级和限制逻辑与「高清」(standard) 完全一致，等级 1 即可使用
- WebSocket `CONFIG_UPDATE` / `onThinConfigReceived` 同步支持 `type: "low"`
- **文件**: `MainPage.qml`

#### 3. 慢放播放/暂停快捷键 Q
- 新增快捷键 `Q` 控制慢放播放/暂停
- 按钮文字显示快捷键提示：`播放(Q)` / `暂停(Q)`
- **文件**: `MainPage.qml`

#### 4. 鼠标左键抓拍
- 实时流画面和慢放画面支持鼠标左键点击触发抓拍（与 `Space` 相同）
- **文件**: `MainPage.qml`

#### 5. 登录窗口显示设备序列号
- 登录窗口顶部居中显示 PC 设备号
- 设备号旁附带「复制」按钮，点击复制到剪贴板
- 设备号从本地生成，无需登录即可显示（解决至尊版到期无法查看设备号的问题）
- **文件**: `LoginPage.qml`, `httpclient.h`, `httpclient.cpp`

#### 6. 安装包支持选择安装目录
- Inno Setup 安装脚本添加 `DisableDirPage=no`，安装时可选择目标目录
- **文件**: `installer.iss`, `pack.bat`

#### 7. iOS 断线自动刷新在线状态
- WebSocket 收到 iOS 设备断线 (`CONFIG_ERROR`) 后，若「切换账号」弹框已打开，自动调用 `/api/binding/online-status` 刷新设备在线状态
- **文件**: `MainPage.qml`

---

### 🔄 功能优化

#### 1. 帧率上限动态化 (levelFps)
- 移除所有挡位的硬编码帧率上限
- 登录接口 `/api/auth/login/control` 新增 `levelFps` 字段（`int[]`），下标 0=试用, 1=高清, 2=超清, 3=超高清, 4=超高帧
- `getMaxFpsForQuality()` 函数统一从 `mainPage.levelFps[level]` 读取 iOS 会员等级帧率上限
- PC 端等级额外限制：豪华版(1)及以下最大 120，至尊版(2)不限制
- **文件**: `MainPage.qml`, `httpclient.h`, `httpclient.cpp`

#### 2. 登录接口集成 deviceLevel
- 后端 `/api/auth/login/control` 新增 `deviceLevel` 字段
- 前端解析并设置 `memberActivationLevel`，即使 iOS 设备离线也能正确显示设备等级
- **文件**: `MainPage.qml`, `LoginPage.qml`, `httpclient.h`, `httpclient.cpp`

#### 3. 切换账号挡位同步
- 切换账号时立即重置挡位为「超清」(high)
- 登录成功后通过 `getThinConfig()` 和 `initWebSocket()` 重新获取正确挡位
- 相机设定弹窗和实时流底部挡位文字始终保持同步
- **文件**: `MainPage.qml`

#### 4. 相机设定弹窗说明文字
- 「还原」按钮后新增红色警告：`网络波动大请先降低：清晰度 → 分辨率 → 帧率`
- 对焦说明：`数值越大远距离图像清楚，数值越小近距离图像清楚`
- 清晰度说明：`数值越高图片像素越高，对网速要求越高，建议远距离使用`
- 帧率说明：`数值越高每秒钟看到图片张数越多，对网速要求越高`
- 超级帧率说明（2 行）：`不影响网速，数值越高每张图片拖影越小画面越暗` / `超过光速会看到光闪，建议配合亮度使用`
- 所有说明文字均为红色 (#FF0000)
- **文件**: `MainPage.qml`

#### 5. 顶部导航栏快捷键提示
- 「相机设定」按钮显示为 `相机设定(R)`
- **文件**: `MainPage.qml`

#### 6. 快捷键说明弹窗更新
- 新增 `P: 相机参数精调`
- 新增 `Q: 慢放播放/暂停`
- 新增 `左键: 抓拍(同Space)`
- 新增 `1-9: 列预览(对应列截图放大)`
- **文件**: `MainPage.qml`

---

### 🐛 Bug 修复

#### 1. GridView 3×7 布局闪烁
- **问题**: 设置 3 行 7 列时，拖动窗口大小会导致截图 view 闪烁并丢失列
- **原因**: QML `GridView` 的 `cellWidth` 浮点精度问题，`Math.floor(width / (width / 7))` 偶尔计算为 6
- **修复**: `cellWidth` 和 `cellHeight` 使用 `Math.floor()` 确保整数值，`Math.max(1, ...)` 防止零值
- **文件**: `MainPage.qml`

#### 2. 慢放播放/暂停无效
- **问题 1**: RECORDING 状态 + 1x 倍速下，暂停后新帧仍然渲染
- **修复**: `updateEndIndex()` 添加 `m_isPlaying` 检查
- **问题 2**: 手动步进帧后（`m_followLive = false`），按播放无效果
- **修复**: `play()` 的 RECORDING + 1x 分支设置 `m_followLive = true` 并跳转到最新帧
- **文件**: `slowmotionplayer.cpp`

#### 3. 设备号登录前为空
- **问题**: `pcDeviceId()` 在登录前返回空字符串
- **修复**: 在 `HttpClient` 构造函数中初始化 `m_pcDeviceId = generatePcDeviceId()`
- **文件**: `httpclient.cpp`

---

### ✏️ 文案修改

#### 1.「管理密码」→「绑定码」
- 切换账号 → 修改密码弹框：所有「管理密码」文字改为「绑定码」
- 密码长度提示改为「1-20位」
- 修改成功提示改为「登录密码和绑定码修改成功」
- **文件**: `MainPage.qml`, `httpclient.cpp`

#### 2. 登录密码长度验证
- PC 端登录密码验证从「6-20位」改为「1-20位」
- 绑定码验证从「6-20位」改为「1-20位」
- **文件**: `LoginPage.qml`, `MainPage.qml`

#### 3.「饱和度」→「红外模式」
- 相机设定弹窗中标签文字变更
- **文件**: `MainPage.qml`

---

### 📝 文档

- 新增 `等级与帧率关系说明.md`：详细说明挡位、等级、帧率三者关系及动态 levelFps 机制
- 新增 `更新日志.md`：本文件

---

### 涉及文件汇总

| 文件 | 改动类型 |
|------|----------|
| `MainPage.qml` | 新功能 / 优化 / Bug修复 / 文案 |
| `LoginPage.qml` | 设备号显示 / 密码验证 / deviceLevel |
| `httpclient.h` | deviceLevel / levelFps / copyToClipboard |
| `httpclient.cpp` | 登录解析 / 剪贴板 / 设备号初始化 |
| `slowmotionplayer.cpp` | 慢放播放暂停修复 |
| `installer.iss` | 安装目录选择 |
| `pack.bat` | 安装脚本 |
